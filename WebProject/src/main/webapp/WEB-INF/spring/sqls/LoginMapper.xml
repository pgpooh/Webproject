<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="LoingId">
<select id="login" resultType="com.mvc.board.model.dto.LoginDto">
SELECT ID, PW FROM membertb WHERE ID = #{login_id} AND PW = #{login_pw}
</select>
</mapper>